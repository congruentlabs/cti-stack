apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.cluster.name }}-bootstrap
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  username: {{ .Values.bootstrap.owner | quote }}
  password: {{ .Values.bootstrap.password | quote }}
