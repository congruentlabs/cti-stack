## ---------------------------------------------------------------------------
## OpenSearch Kubernetes Operator — subchart values
## Deploys only the operator manager; OpenSearchCluster CRs are in per-app charts
## ---------------------------------------------------------------------------
opensearch-operator:
  manager:
    resources:
      requests:
        memory: 256Mi
        cpu: 100m
      limits:
        memory: 512Mi
