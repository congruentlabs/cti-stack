kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      retention: 30d
      replicas: 2
      resources:
        requests:
          memory: 4Gi
          cpu: "1"
        limits:
          memory: 8Gi
      storageSpec:
        volumeClaimTemplate:
          spec:
            storageClassName: longhorn
            resources:
              requests:
                storage: 200Gi
  alertmanager:
    alertmanagerSpec:
      replicas: 2
  grafana:
    replicas: 2
    resources:
      requests:
        memory: 512Mi
      limits:
        memory: 1Gi

loki:
  deploymentMode: SingleBinary
  singleBinary:
    replicas: 1
  read:
    replicas: 0
  write:
    replicas: 0
  backend:
    replicas: 0
  loki:
    commonConfig:
      replication_factor: 1
